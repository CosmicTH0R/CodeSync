# MongoDB Connection
# For local development with Docker:
MONGODB_URI=mongodb+srv://mdsalman59765976_db_user:Salman5976@cluster0.egynsa8.mongodb.net/?appName=Cluster0

# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/codesync?retryWrites=true&w=majority

# Server ports
HTTP_PORT=4000
WS_PORT=1234

# Client origin for CORS
CLIENT_ORIGINS=http://localhost:5173

# Judge0 API key (if using Judge0 instead of local execution)
JUDGE0_API_KEY=demo
