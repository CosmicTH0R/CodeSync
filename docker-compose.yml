version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    container_name: codesync-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: codesync
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

volumes:
  mongo_data:
